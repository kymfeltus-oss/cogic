import {redirect} from "next/navigation";import {getUserFromSession} from "@/lib/auth/session";import HousingExperience from "@/components/housing/HousingExperience";export const dynamic="force-dynamic";export default async function Page(){if(!await getUserFromSession())redirect("/login?next=%2Fhousing");return <main className="min-h-dvh bg-black p-5 text-white"><div className="mx-auto max-w-5xl"><HousingExperience/></div></main>}
